<UserControl x:Class="WpfPrismLearn.Views.SimpleModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="300" Height="200">
    
    <materialDesign:Card Padding="0" UniformCornerRadius="8" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ContentControl Grid.Row="0"
                         prism:RegionManager.RegionName="ModalHeaderRegion"/>

            <ContentControl Grid.Row="1"
                         prism:RegionManager.RegionName="ModalContentRegion"/>

            <ContentControl Grid.Row="2"
                         prism:RegionManager.RegionName="ModalFooterRegion"/>
        </Grid>
    </materialDesign:Card>
</UserControl>
