<UserControl x:Class="WpfPrismLearn.Views.DetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            
            <materialDesign:PackIcon Kind="Information" Width="40" Height="40" 
                                     HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

            <TextBlock Text="選択された画像詳細" Margin="0,10,0,10" FontWeight="Bold" HorizontalAlignment="Center"/>

            <Image Source="{Binding SelectedImage.ImageUrl}" 
                   Height="150" 
                   Stretch="Uniform" 
                   Margin="0,0,0,10"/>

            <TextBlock Text="{Binding SelectedImage.Title}" 
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       HorizontalAlignment="Center"/>

        </StackPanel>
    </Grid>
</UserControl>
