<UserControl x:Class="WpfPrismLearn.Views.DetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <materialDesign:PackIcon Kind="Information" Width="50" Height="50" 
                                     HorizontalAlignment="Center" Foreground="Blue"/>
            <TextBlock Text="これは詳細画面です" Margin="0,10,0,0" FontWeight="Bold"/>
            <TextBlock Text="{Binding ModalMessage}" Opacity="0.5"/>
        </StackPanel>
    </Grid>
</UserControl>
